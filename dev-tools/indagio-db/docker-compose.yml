version: "3.9"

services:
  postgres:
    image: postgres:16
    container_name: indagio_postgres_local
    ports:
      - "5492:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: indagio_db
    volumes:
      - db_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d indagio_db"]
      interval: 5s
      timeout: 3s
      retries: 10

volumes:
  db_data:
